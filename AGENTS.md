# Dora 开发规则

## 项目目标

Dora 是一个仅供个人使用、运行在 macOS 本地的 AI 编程用量管理工具。

开发范围：

- 第一期：本地 Web 仪表盘、SQLite、Codex token 使用量和订阅配额。
- 第二期：macOS 菜单栏、Claude Code token 使用量和订阅配额。

详细设计见：

- `docs/dora-development-guide.md`

开始开发前，阅读与当前功能相关的章节。

## 开发原则

- 优先让功能端到端跑通。
- 这是单用户、本地运行的个人项目。
- token 使用量和订阅配额同等重要。
- 保持前后端分离。
- 优先使用简单、直接、可维护的实现。
- 不提前开发当前阶段之外的功能。
- 不为假想需求增加抽象、兼容层或复杂框架。
- 不按照多租户、分布式或大型企业系统设计。
- 遇到普通技术选择时，由 Agent 自主决定。
- 用户只负责最终功能验收。

简单实现不能破坏：

- token 统计准确性；
- 配额时间窗口准确性；
- SQLite 数据完整性；
- 增量扫描去重；
- 已有用户数据。

## 编码风格

- 保持实现简洁、清晰、可读，优先选择最直接且满足当前需求的方案。
- 不进行过度防御编程。只在外部输入、文件解析、数据库操作和可能破坏已有数据的边界进行必要校验；内部逻辑遵循已经建立的类型、接口和调用约定。
- 错误处理应明确且有实际恢复价值，不添加无意义的兜底、重复校验、静默忽略或永远不会触发的分支。
- 注释使用精简中文，重点解释设计原因、统计口径、去重逻辑、时间窗口和不直观的约束，不复述代码本身。
- 优先通过清晰的命名和简单的控制流表达意图，不能用注释掩盖复杂或含糊的实现。
- 保持项目结构整洁。只有形成真实、稳定的职责边界时才拆分目录、文件、接口或公共抽象。
- 避免超大文件和超长函数，但不为了形式上的分层创建只有一层转发的包装、工具类或空目录。
- 新增代码应延续所在模块的命名、组织和错误处理风格。
- 删除已经失效的代码、注释和兼容逻辑，不长期保留“以后可能用到”的实现。
- 避免散落的硬编码。端口、时间窗口、产品日期、外部地址、容量限制和重复出现的视觉值应使用命名常量、配置或设计 token 表达；安全边界必须保持显式且集中。
- 不为了消除单个直观字面量而增加无意义抽象。只有值具有业务含义、环境差异、复用需求或修改风险时才提取。
- Dora Web 前端只面向 macOS 桌面浏览器，布局、交互、视觉效果和浏览器验收只需考虑 PC 桌面视口。
- 除非用户明确要求，不开发手机或平板响应式布局、移动端导航、触摸交互优化和移动端专项测试，也不为移动端兼容增加 CSS breakpoint 或组件分支。

## 项目结构

项目至少保持以下结构：

```text
backend/
frontend/
docs/
```

职责划分：

- `backend/`：数据采集、数据解析、SQLite、统计逻辑、配额逻辑和 API。
- `frontend/`：页面、交互和数据展示。
- `docs/`：产品设计、开发指南和开发进度。

前端只能通过 API 获取数据，不得直接读取 SQLite 或解析 Agent 原始文件。

后端内部按真实功能逐步拆分，不要在项目初始化时创建大量空目录。

暂时不创建子目录 `AGENTS.md`。只有当某个目录出现独立的开发规则、测试命令或依赖边界时再添加。

## Agent 自主权限

开发过程中，Agent 自主完成：

- 代码检索；
- 方案选择；
- 编码；
- 安装依赖；
- 数据库 migration；
- 测试；
- 调试；
- 启动本地服务；
- 页面验证；
- Code Review；
- 文档更新；
- Git commit；
- 推送 GitHub。

普通开发过程不需要用户进行中间确认。

仅在以下情况询问用户：

- 缺少的信息会明显改变产品功能；
- 不同选择会改变 token 或配额统计口径；
- 可能造成无法恢复的数据丢失；
- 外部账号必须由用户亲自授权；
- 外部系统阻止任务继续进行。

## 功能开发流程

每个可独立验收的功能点按照以下流程完成：

1. 阅读相关代码和开发指南。
2. 检查并保留用户已有修改。
3. 明确功能验收标准。
4. 完成功能实现。
5. 运行相关测试。
6. 完成端到端自测。
7. 由 Agent 完成 Code Review。
8. 修复 Review 发现的问题。
9. 重新运行相关测试。
10. 更新必要文档。
11. 创建一个独立 commit。
12. 推送当前分支到 GitHub。
13. 向用户提供功能验收步骤。

如果一个任务包含多个可以独立验收的功能点，应分别完成、测试、Review、提交和推送。

## 测试与 Code Review

测试范围与改动风险匹配，不追求没有实际价值的测试数量。

token 和配额功能重点验证：

- 不重复计数；
- 增量更新正确；
- 时间范围和本地时区正确；
- token 类型没有混淆；
- 配额重置时间正确；
- 数据源不可用时不会破坏已有数据。

完成编码后，优先使用独立 Reviewer Agent 检查当前 diff。

如果独立 Reviewer Agent 不可用，则执行一次与编码过程分离的自审。

Review 重点：

- 功能是否满足验收标准；
- token 和配额统计是否正确；
- 是否存在重复计算；
- SQLite 数据是否安全；
- 前后端职责是否混乱；
- 是否存在不必要的抽象；
- 是否混入无关修改；
- 测试是否覆盖真实行为。

发现的有效问题必须在提交前修复。

## Git 与 GitHub

用户授权 Agent 在每个功能点自测和 Review 完成后自动执行：

- `git add`
- `git commit`
- `git push`

一次 commit 只包含一个功能点。

提交信息使用英文。

每次 commit 除英文标题外，还应包含一段简短的英文正文：

- 正文通常为 1～3 句，概述主要改动和必要原因。
- 保持简洁，不重复标题，不罗列实现流水账。
- 极小改动也至少提供一句简要说明。

由 Codex 参与完成的提交保留用户配置的 Git 作者信息，并在提交信息末尾添加：

```text
Co-authored-by: Codex <noreply@openai.com>
```

提交信息优先使用：

- `feat(scope): description`
- `fix(scope): description`
- `refactor(scope): description`
- `test(scope): description`
- `docs(scope): description`

推送当前分支：

```bash
git push origin HEAD
```

如果没有上游分支：

```bash
git push -u origin HEAD
```

禁止：

- 强制推送；
- 重写已经推送的历史；
- 覆盖用户已有修改；
- 提交与当前功能无关的文件；
- 提交 `.env`、凭证、SQLite 数据库或原始会话数据；
- 测试失败时仍然提交。

推送失败时由 Agent 自行诊断。无法安全解决时，在最终结果中报告原因和本地 commit hash。

## 完成标准

一个功能点只有满足以下条件才算完成：

- 功能可以实际运行；
- 自动测试通过；
- 端到端自测完成；
- Agent Code Review 完成；
- Review 问题已经修复；
- 必要文档已经更新；
- 已创建独立 commit；
- 已推送 GitHub；
- 已向用户提供明确的验收步骤。

最终回复包含：

- 实现内容；
- 自测结果；
- Code Review 结论；
- commit hash；
- 推送分支；
- 用户验收步骤。
